import React from "react";

// Components
import Bar from "../../components/Bar";

const Store = () => {
  return (
    <div className="store">
      Store
      <Bar />
    </div>
  );
};

export default Store;
